# Bootstrap Generic Renderer Module Plan

- Start with a root-only module shape.
- Reuse generic rendering patterns from `terraform-tfe-cloud/modules/workspace`.
- Keep runtime/driver-specific behaviors out of this repo.
- Validate with one executable example.
