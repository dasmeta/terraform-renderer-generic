# Tasks

- [x] Create `modules/infra-yaml-loader` submodule (variables, locals, outputs, versions).
- [x] Implement shared-config merge and workspace filtering rules.
- [x] Implement linked workspace auto-detection from interpolation.
- [x] Add example fixtures and check blocks.
- [x] Document submodule usage in module and root README files.
- [ ] Publish new renderer version via semantic release on merge to `main`.
- [ ] Update driver modules to use registry source `dasmeta/generic/renderer//modules/infra-yaml-loader`.
